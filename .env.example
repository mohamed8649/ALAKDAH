# PostgreSQL connection string.
DATABASE_URL="postgresql://user:password@localhost:5432/alakdah?schema=public"

# 32+ random bytes. Signs session cookies.
SESSION_SECRET=""

# 32+ random bytes. AES-256-GCM key for integration credentials.
ENCRYPTION_KEY=""

# "mock" (deterministic, no network) or "anthropic".
AI_PROVIDER="mock"
ANTHROPIC_API_KEY=""

NEXT_PUBLIC_APP_URL="http://localhost:3000"
UPLOAD_DIR="./public/uploads"
